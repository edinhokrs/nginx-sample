apiVersion: v1
kind: Service
metadata:
  name: {{ include "edison-krause.fullname" . }}
  labels:
    {{- include "edison-krause.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edison-krause.selectorLabels" . | nindent 4 }}
